import 'package:flutter/material.dart';

class SearchDesktopLayout extends StatelessWidget {
  const SearchDesktopLayout({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text("Hi PC ppl"),
    );
  }
}
