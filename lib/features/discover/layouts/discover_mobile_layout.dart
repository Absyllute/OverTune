import 'package:flutter/material.dart';

class DiscoverMobileLayout extends StatelessWidget {
  const DiscoverMobileLayout({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text("Discover Mobile Layout"),
    );
  }
}
